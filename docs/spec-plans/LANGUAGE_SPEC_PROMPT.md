# SPLOOSH Quick Reference — Retired (v0.5.8)

The combined prompt-sized reference has been split into two artifacts. Load the one(s) you need:

- **`LANGUAGE_SPEC_PROMPT_CORE.md`** — language core (syntax, types, ownership, math, actors, errors, iterators, modules, runtime, async, FFI, attributes, testing, diagnostics, manifest) plus the "Common LLM Mistakes" appendix.
- **`LANGUAGE_SPEC_PROMPT_WEB3.md`** — on-chain surface (§11): `onchain mod`, `storage`, `ctx`, `chain::call`, storage layout, reentrancy guard, gas/CU model, events, `@payable` / `@reentrant`.

For on-chain work, load both. The on-chain prohibition list is duplicated in both files for prompt-locality.

The full normative source remains `LANGUAGE_SPEC.md`.
