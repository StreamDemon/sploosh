# std::time

> Timestamps, durations, and timers.

**Available targets:** native, wasm

Not available inside `onchain` modules (compile-time error). Use `ctx::timestamp()` for chain time.

<!-- TODO: Document API once implemented -->
