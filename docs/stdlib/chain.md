# std::chain

> Web3 utilities, ABI encoding, and address handling.

**Available targets:** all (including onchain)

```sploosh
use std::chain;

let result = chain::call(token_addr, token::balance_of, sender)?;
```

<!-- TODO: Document full API (ABI encoding/decoding, address validation, chain utilities) once implemented -->
